# Sao chép file này thành .env rồi điền giá trị thật (KHÔNG commit file .env)
# cp .env.example .env

# API key của OpenAI - lấy tại https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here
